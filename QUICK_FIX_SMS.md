# حل سريع: Double Shake لا يرسل SMS

## 🔍 خطوات التحقق السريعة

### 1. **افتح Logcat في Android Studio**
   - View → Tool Windows → Logcat
   - Filter: `SOSUtil` أو `GestureDetector`

### 2. **تحقق من Double Shake**
   - هز الهاتف مرتين
   - يجب أن ترى في Logcat:
     ```
     GestureDetector: *** DOUBLE SHAKE DETECTED! ***
     GestureService: *** DOUBLE SHAKE DETECTED - SENDING SOS ***
     SOSUtil: === sendSOSAlert() called ===
     ```

### 3. **تحقق من جهات الاتصال**
   - يجب أن ترى:
     ```
     SOSUtil: Found X emergency contact(s)
     SOSUtil: Contact: [اسم] - Phone: [رقم]
     ```

### 4. **تحقق من إرسال SMS**
   - يجب أن ترى:
     ```
     SOSUtil: Attempting to send SMS to: [رقم]
     SOSUtil: SMS sent successfully to [اسم]
     ```

---

## ⚠️ المشاكل الشائعة والحلول

### **المشكلة 1: "No emergency contacts found"**

**السبب:** لا توجد جهات اتصال في قاعدة البيانات

**الحل:**
1. اذهب إلى "Emergency Contacts"
2. اضغط على (+)
3. أدخل:
   - Name: أي اسم
   - Phone: **8 أرقام** (مثل: `12345678`)
4. احفظ

---

### **المشكلة 2: "SMS permission NOT granted"**

**السبب:** صلاحية SMS غير ممنوحة

**الحل:**
1. Settings → Apps → Your App → Permissions
2. فعّل "SMS"
3. أو أعد تشغيل التطبيق وامنح الصلاحية

---

### **المشكلة 3: رقم الهاتف 8 أرقام فقط**

**المشكلة:** رقم الهاتف `12345678` قد لا يعمل بدون كود الدولة

**الحل المدمج:**
الكود يضيف `+216` تلقائياً → `+21612345678`

**لكن إذا كان رقمك من بلد آخر:**
- أضف الرقم كاملاً عند الإدخال: `+33XXXXXXXX` (مثال لفرنسا)
- أو غيّر `+216` في الكود إلى كود بلدك

---

### **المشكلة 4: لا توجد إشارة شبكة**

**السبب:** SMS يحتاج شبكة اتصالات (GSM/CDMA)

**الحل:**
- تأكد من وجود SIM Card
- تأكد من وجود إشارة شبكة
- جرب إرسال SMS يدوياً للتأكد

---

## 🧪 اختبار سريع

### **الخطوات:**

1. **أضف جهة اتصال:**
   ```
   Name: Test
   Phone: [رقمك الشخصي - 8 أرقام]
   ```

2. **افتح Logcat:**
   - Filter: `SOSUtil`

3. **هز الهاتف مرتين بقوة**

4. **تحقق من Logcat:**
   - يجب أن ترى جميع الخطوات
   - يجب أن ترى "SMS sent successfully"

5. **تحقق من هاتفك:**
   - يجب أن تستقبل SMS على الرقم الذي أدخلته

---

## 📋 Checklist كامل

- [ ] Gesture Control مفعّل (Status: Active)
- [ ] يوجد إشعار "Gesture Control Active" في شريط الإشعارات
- [ ] Double Shake يتم اكتشافه (في Logcat)
- [ ] يوجد جهات اتصال طارئة (في Emergency Contacts)
- [ ] صلاحية SMS ممنوحة (في Settings)
- [ ] رقم الهاتف صحيح (8 أرقام)
- [ ] يوجد SIM Card وإشارة شبكة
- [ ] SMS يُرسل بنجاح (في Logcat)

---

## 🆘 إذا لم يعمل بعد

**انسخ Logcat output وأرسله:**
1. افتح Logcat
2. Filter: `SOSUtil` أو `GestureDetector` أو `GestureService`
3. هز الهاتف مرتين
4. انسخ جميع الرسائل
5. أرسلها لأرى المشكلة بالضبط

**أو ابحث عن:**
- `Exception`
- `Error`
- `Failed`
- `permission`
- `contact`

